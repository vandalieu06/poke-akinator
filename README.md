# POKE AKINATOR

## Inizializar el proyecto

```bash
python -m venv .venv
pip install -r requeriments.txt
python -m app.main
```

## Dockerfile + Docker Compose

```bash
docker compose up -d
```

## Web UI

Application run in: **localhost:3000**
